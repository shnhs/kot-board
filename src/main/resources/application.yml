spring:
    application:
        name: kotboard
    datasource:
        url: jdbc:mysql://localhost:3307/kotboard?useSSL=false&serverTimezone=Asia/Seoul
        username: root
        password: root
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
            hibernate:
                format-sql: true


